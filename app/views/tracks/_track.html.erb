<tr class="<%= cycle('list_line_odd', 'list_line_even') %>" >
  <th style="width:20%"><%= track.title %></th>
  <td style="width:90%"><%= track.description %></td>
  <td style="width:5%"><%= link_to "Show", project_track_path(track.project, track) %></td>
  <td style="width:5%"><%= link_to "Edit", edit_project_track_path(track.project, track) %></td>
</tr>
