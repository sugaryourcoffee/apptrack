<%= render 'form' %>

<%= content_for :secondary_nav do %>
  <%= breadcrumbs [ link_to("Projects", projects_path),
                    link_to(@project.title, project_path(@project)),
                    link_to(truncate(@track.title), 
                            project_track_path(@project, @track)),
                    "Edit" ] %>
<% end %>
